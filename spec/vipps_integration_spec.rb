# frozen_string_literal: true

RSpec.describe VippsIntegration do
  it "has a version number" do
    expect(VippsIntegration::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
